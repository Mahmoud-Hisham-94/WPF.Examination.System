<Window x:Class="ExaminationSys.QuestionPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ExaminationSys"
        mc:Ignorable="d"
        Title="Question Preview" Height="500" Width="600"
        WindowStartupLocation="CenterOwner"
        Background="#F5F5F5"
        ResizeMode="NoResize">
    
    <Window.Resources>
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
        
        <Style x:Key="BodyTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#555"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>
        
        <Style x:Key="InfoTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#666"/>
            <Setter Property="Margin" Value="0,2"/>
        </Style>
        
        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1976D2"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#1565C0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2E7D32" Padding="20">
            <TextBlock Text="👁️ Question Preview" 
                      Style="{StaticResource HeaderTextStyle}"
                      Foreground="White"
                      HorizontalAlignment="Center"/>
        </Border>

        <!-- Main Content -->
        <ScrollViewer Grid.Row="1" Margin="20" VerticalScrollBarVisibility="Auto">
            <Border Background="White" 
                   BorderBrush="#E0E0E0" 
                   BorderThickness="1" 
                   CornerRadius="8" 
                   Padding="20">
                <StackPanel>
                    <TextBlock x:Name="PreviewHeader" 
                              Style="{StaticResource HeaderTextStyle}"/>
                    
                    <TextBlock x:Name="PreviewBody" 
                              Style="{StaticResource BodyTextStyle}"/>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                        <TextBlock Text="Type: " 
                                  FontWeight="Bold" 
                                  FontSize="12"/>
                        <TextBlock x:Name="PreviewType" 
                                  Style="{StaticResource InfoTextStyle}"/>
                    </StackPanel>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                        <TextBlock Text="Marks: " 
                                  FontWeight="Bold" 
                                  FontSize="12"/>
                        <TextBlock x:Name="PreviewMarks" 
                                  Style="{StaticResource InfoTextStyle}"/>
                    </StackPanel>
                    
                    <TextBlock Text="Choices:" 
                              FontWeight="Bold" 
                              FontSize="12" 
                              Margin="0,0,0,5"/>
                    <TextBlock x:Name="PreviewChoices" 
                              Style="{StaticResource InfoTextStyle}"/>
                    
                    <TextBlock Text="Correct Answer(s):" 
                              FontWeight="Bold" 
                              FontSize="12" 
                              Margin="0,10,0,5"/>
                    <TextBlock x:Name="PreviewCorrectAnswers" 
                              FontSize="12" 
                              Foreground="#4CAF50" 
                              FontWeight="Bold"/>
                </StackPanel>
            </Border>
        </ScrollViewer>

        <!-- Close Button -->
        <Border Grid.Row="2" Background="#E8F5E8" Padding="20">
            <StackPanel HorizontalAlignment="Center">
                <Button x:Name="CloseButton" 
                       Content="Close Preview" 
                       Style="{StaticResource CloseButtonStyle}"
                       Click="CloseButton_Click"
                       Width="120"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>

