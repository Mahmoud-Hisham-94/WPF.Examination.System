<Window x:Class="ExaminationSys.ExamSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ExaminationSys"
        mc:Ignorable="d"
        Title="Exam Settings" Height="500" Width="600"
        WindowStartupLocation="CenterOwner"
        Background="#F5F5F5"
        ResizeMode="NoResize">
    
    <Window.Resources>
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>
        
        <Style x:Key="SectionTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#2E7D32"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
        
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>
        
        <Style x:Key="TextBoxStyle" TargetType="TextBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                        Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1976D2"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#1565C0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="SuccessButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#45a049"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#3d8b40"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2E7D32" Padding="20">
            <TextBlock Text="⚙️ Exam Settings" 
                      Style="{StaticResource HeaderTextStyle}"
                      Foreground="White"
                      HorizontalAlignment="Center"/>
        </Border>

        <!-- Main Content -->
        <ScrollViewer Grid.Row="1" Margin="20" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Practice Exam Settings -->
                <Border Background="White" 
                       BorderBrush="#E0E0E0" 
                       BorderThickness="1" 
                       CornerRadius="8" 
                       Padding="20" 
                       Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="📚 Practice Exam Settings" 
                                  Style="{StaticResource SectionTextStyle}"/>
                        
                        <TextBlock Text="Time Limit (minutes):" 
                                  Style="{StaticResource LabelStyle}"/>
                        <TextBox x:Name="PracticeTimeTextBox" 
                                Style="{StaticResource TextBoxStyle}"
                                Text="60"/>
                        
                        <TextBlock Text="Current Questions: " 
                                  FontSize="12" 
                                  FontWeight="Bold"/>
                        <TextBlock x:Name="PracticeQuestionsCount" 
                                  FontSize="12" 
                                  Foreground="#666" 
                                  Margin="0,0,0,10"/>
                    </StackPanel>
                </Border>

                <!-- Final Exam Settings -->
                <Border Background="White" 
                       BorderBrush="#E0E0E0" 
                       BorderThickness="1" 
                       CornerRadius="8" 
                       Padding="20" 
                       Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="🎯 Final Exam Settings" 
                                  Style="{StaticResource SectionTextStyle}"/>
                        
                        <TextBlock Text="Time Limit (minutes):" 
                                  Style="{StaticResource LabelStyle}"/>
                        <TextBox x:Name="FinalTimeTextBox" 
                                Style="{StaticResource TextBoxStyle}"
                                Text="90"/>
                        
                        <TextBlock Text="Current Questions: " 
                                  FontSize="12" 
                                  FontWeight="Bold"/>
                        <TextBlock x:Name="FinalQuestionsCount" 
                                  FontSize="12" 
                                  Foreground="#666" 
                                  Margin="0,0,0,10"/>
                    </StackPanel>
                </Border>

                <!-- Additional Settings -->
                <Border Background="White" 
                       BorderBrush="#E0E0E0" 
                       BorderThickness="1" 
                       CornerRadius="8" 
                       Padding="20">
                    <StackPanel>
                        <TextBlock Text="🔧 Additional Settings" 
                                  Style="{StaticResource SectionTextStyle}"/>
                        
                        <CheckBox x:Name="AutoSubmitCheckBox" 
                                 Content="Auto-submit exam when time expires" 
                                 FontSize="14" 
                                 IsChecked="True" 
                                 Margin="0,0,0,10"/>
                        
                        <CheckBox x:Name="ShowCorrectAnswersCheckBox" 
                                 Content="Show correct answers in practice exam" 
                                 FontSize="14" 
                                 IsChecked="True" 
                                 Margin="0,0,0,10"/>
                        
                        <CheckBox x:Name="AllowNavigationCheckBox" 
                                 Content="Allow question navigation (Previous/Next)" 
                                 FontSize="14" 
                                 IsChecked="True" 
                                 Margin="0,0,0,10"/>
                        
                        <TextBlock Text="These settings will be applied to all new exam sessions." 
                                  FontSize="12" 
                                  Foreground="#666" 
                                  TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Action Buttons -->
        <Border Grid.Row="2" Background="#E8F5E8" Padding="20">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="SaveButton" 
                       Content="💾 Save Settings" 
                       Style="{StaticResource SuccessButtonStyle}"
                       Click="SaveButton_Click"/>
                
                <Button x:Name="ResetButton" 
                       Content="🔄 Reset to Defaults" 
                       Style="{StaticResource ActionButtonStyle}"
                       Click="ResetButton_Click"/>
                
                <Button x:Name="CancelButton" 
                       Content="❌ Cancel" 
                       Style="{StaticResource ActionButtonStyle}"
                       Click="CancelButton_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>

