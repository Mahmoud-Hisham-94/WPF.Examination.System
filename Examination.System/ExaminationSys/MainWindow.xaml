<Window x:Class="ExaminationSys.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ExaminationSys"
        mc:Ignorable="d"
        Title="Examination System Pro" Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">
    
    <Window.Resources>
        <Style x:Key="MainButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="20,15"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#45a049"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#3d8b40"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1976D2"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#1565C0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2E7D32" Padding="20">
            <StackPanel>
                <TextBlock Text="Examination System Pro" 
                          FontSize="32" 
                          FontWeight="Bold" 
                          Foreground="White" 
                          HorizontalAlignment="Center"/>
                <TextBlock Text="Advanced C# Examination Platform" 
                          FontSize="16" 
                          Foreground="#E8F5E8" 
                          HorizontalAlignment="Center" 
                          Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="40">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Welcome Message -->
            <StackPanel Grid.Row="0" Margin="0,0,0,30">
                <TextBlock Text="Welcome to the Examination System!" 
                          FontSize="24" 
                          FontWeight="SemiBold" 
                          Foreground="#333" 
                          HorizontalAlignment="Center"/>
                <TextBlock Text="Choose an option below to get started" 
                          FontSize="14" 
                          Foreground="#666" 
                          HorizontalAlignment="Center" 
                          Margin="0,10,0,0"/>
            </StackPanel>

            <!-- Main Buttons -->
            <StackPanel Grid.Row="1" VerticalAlignment="Center">
                <Button x:Name="PracticeExamButton" 
                       Content="📚 Take Practice Exam" 
                       Style="{StaticResource MainButtonStyle}"
                       Click="PracticeExamButton_Click"
                       Width="300"/>
                
                <Button x:Name="FinalExamButton" 
                       Content="🎯 Take Final Exam" 
                       Style="{StaticResource MainButtonStyle}"
                       Click="FinalExamButton_Click"
                       Width="300"/>
                
                <Button x:Name="ViewQuestionsButton" 
                       Content="📋 View Questions" 
                       Style="{StaticResource SecondaryButtonStyle}"
                       Click="ViewQuestionsButton_Click"
                       Width="300"/>
            </StackPanel>

            <!-- Status Bar -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" 
                       HorizontalAlignment="Center" 
                       Margin="0,30,0,0">
                <TextBlock x:Name="StatusText" 
                          Text="Ready to start your examination" 
                          FontSize="12" 
                          Foreground="#666"/>
            </StackPanel>
        </Grid>

        <!-- Footer -->
        <Border Grid.Row="2" Background="#E8F5E8" Padding="20">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="© 2024 Examination System Pro | " 
                          FontSize="12" 
                          Foreground="#666"/>
                <TextBlock Text="Advanced C# Labs" 
                          FontSize="12" 
                          Foreground="#666"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
