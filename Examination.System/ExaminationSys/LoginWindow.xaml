<Window x:Class="ExaminationSys.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ExaminationSys"
        mc:Ignorable="d"
        Title="Examination System - Login" Height="800" Width="400"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5" SizeToContent="WidthAndHeight">

    <Window.Resources>
        <Style x:Key="LoginButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1976D2"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#1565C0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="StudentButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#45a049"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#3d8b40"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TextBoxStyle" TargetType="TextBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                        Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="21*"/>
            <ColumnDefinition Width="4*"/>
            <ColumnDefinition Width="0*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" MinHeight="0"/>
            <RowDefinition/>
            <RowDefinition Height="Auto" MinHeight="0"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2E7D32" Padding="20" Margin="0,0,0,332" Grid.RowSpan="2" Grid.ColumnSpan="2">
            <StackPanel>
                <TextBlock Text="Examination System Pro" 
                          FontSize="24" 
                          FontWeight="Bold" 
                          Foreground="White" 
                          HorizontalAlignment="Center"/>
                <TextBlock Text="Please select your role" 
                          FontSize="14" 
                          Foreground="#E8F5E8" 
                          HorizontalAlignment="Center" 
                          Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <StackPanel Grid.Row="1" Margin="40,0,40,0" VerticalAlignment="Center" Height="574" Grid.ColumnSpan="2">
            <!-- Student Login -->
            <Border Background="White" 
                   BorderBrush="#E0E0E0" 
                   BorderThickness="1" 
                   CornerRadius="8" 
                   Padding="30" 
                   Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="👨‍🎓 Student Access" 
                              FontSize="20" 
                              FontWeight="Bold" 
                              Foreground="#2E7D32" 
                              HorizontalAlignment="Center" 
                              Margin="0,0,0,15"/>
                    <TextBlock Text="Access practice and final exams" 
                              FontSize="14" 
                              Foreground="#666" 
                              HorizontalAlignment="Center" 
                              Margin="0,0,0,20"/>
                    <Button x:Name="StudentLoginButton" 
                           Content="Continue as Student" 
                           Style="{StaticResource StudentButtonStyle}"
                           Click="StudentLoginButton_Click"/>
                </StackPanel>
            </Border>

            <!-- Teacher Login -->
            <Border Background="White" 
                   BorderBrush="#E0E0E0" 
                   BorderThickness="1" 
                   CornerRadius="8" 
                   Padding="30">
                <StackPanel>
                    <TextBlock Text="👨‍🏫 Teacher Access" 
                              FontSize="20" 
                              FontWeight="Bold" 
                              Foreground="#2196F3" 
                              HorizontalAlignment="Center" 
                              Margin="0,0,0,15"/>
                    <TextBlock Text="Manage questions and exam settings" 
                              FontSize="14" 
                              Foreground="#666" 
                              HorizontalAlignment="Center" 
                              Margin="0,0,0,20"/>

                    <StackPanel>
                        <TextBlock Text="Username:" 
                                  FontSize="12" 
                                  FontWeight="Bold" 
                                  Foreground="#333" 
                                  Margin="0,0,0,5"/>
                        <TextBox x:Name="UsernameTextBox" 
                                FontSize="14" 
                                Padding="10" 
                                BorderThickness="1"
                                BorderBrush="#CCCCCC" 
                                Background="White" 
                                Margin="0,0,0,20" Height="40" Width="250"/>

                        <TextBlock Text="Password:" 
                                  FontSize="12" 
                                  FontWeight="Bold" 
                                  Foreground="#333" 
                                  Margin="0,0,0,5"/>
                        <PasswordBox x:Name="PasswordBox" 
                                    FontSize="14" 
                                    Padding="10" 
                                    BorderThickness="1" 
                                    BorderBrush="#CCCCCC" 
                                    Background="White" 
                                    Margin="0,0,0,20" Height="40" Width="250"/>

                        <Button x:Name="TeacherLoginButton" 
                               Content="Login as Teacher" 
                               Style="{StaticResource LoginButtonStyle}"
                               Click="TeacherLoginButton_Click"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- Footer -->
        <Border Grid.Row="1" Background="#E8F5E8" Padding="20" Margin="0,723,0,0" Grid.ColumnSpan="2">
            <StackPanel HorizontalAlignment="Center">
                <TextBlock Text="© 2024 Examination System Pro | Advanced C# Labs" 
                          FontSize="12" 
                          Foreground="#666"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>

